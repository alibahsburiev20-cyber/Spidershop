# Set these in Cloudflare Pages → Settings → Environment variables (Production)
SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...        # public; served to browser via /api/config
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi... # secret; never sent to browser

PLATFORM_TON_ADDRESS=UQB_y7iS9NCOaRKaSd-ze2voG0qIdDThhT7x3JemIcOqNcZD
PLATFORM_FEE_PERCENT=5

# Optional — tonapi.io API token (without it works with rate limit)
TONAPI_TOKEN=

# Optional — Resend for email receipts
RESEND_API_KEY=
RESEND_FROM=Spidershop <onboarding@resend.dev>
